spring:
  main:
    web-application-type: NONE
strongbox:
  db:
    janus-graph:
      storage-username: cassandra
      storage-password: cassandra
      storage-host: 127.0.0.1
      storage-port: 49142
      storage-root: ./target
logging:
  level:
    liquibase: INFO